import type { Config } from 'tailwindcss';

// Minimal Tailwind preset for the template.
// Extend this in real projects.
const preset: Partial<Config> = {
  theme: { extend: {} },
  plugins: [],
};

export default preset;
