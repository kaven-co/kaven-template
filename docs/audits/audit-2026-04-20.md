# Kaven Framework — Auditoria Técnica Independente
**Data:** 2026-04-20  
**Agents:** Atlas (Arquitetura) · Shield (Segurança/QA) · Schema (DB/Migrations) · Deploy (Perf/DevOps)  
**Metodologia:** 4 agents paralelos, leitura direta do código sem especulação

---

## 🔴 CRITICAL (ação imediata)

| # | Finding | Arquivo | Linha |
|---|---------|---------|-------|
| C-1 | `console.log` vaza fingerprint do JWT secret em todo request autenticado em prod | `apps/api/src/lib/jwt.ts` | 41 |
| C-2 | `STRIPE_SECRET_KEY` lido via `process.env` direto e injetado em header HTTP — bypassa Zod, risco de leak em logs | `apps/api/src/modules/observability/services/external-api-monitor.service.ts` | 240–246 |

---

## 🟠 HIGH

| # | Finding | Arquivo |
|---|---------|---------|
| H-1 | `authorization.service.ts:34` instancia `new PrismaClient()` próprio — segunda connection pool em prod | `apps/api/src/services/authorization.service.ts` |
| H-2 | 20+ arquivos acessam `process.env.*` diretamente bypassando validação Zod (`META_CAPI_ACCESS_TOKEN`, `AWS_SES_*`, `STORAGE_*`, etc.) | múltiplos em `modules/` e `lib/` |
| H-3 | `encryption.util.decrypt()` catch retorna `''` silenciosamente — dado corrompido/adulterado tratado como string válida | `apps/api/src/utils/encryption.util.ts:71-74` |
| H-4 | `seed.ts:417-441` — personas com fallback `"ChangeMe123!"` quando env não configurada | `packages/database/prisma/seed.ts` |
| H-5 | 5 `describe.skip` em suites GDPR + multi-tenant isolation — CI passa verde sem executar compliance tests | `apps/api/test/compliance/` + `test/multi-tenant/` |
| H-6 | `bcrypt` + `bcryptjs` ambos instalados e usados em arquivos diferentes — inconsistência de hashing | `apps/api/package.json` |
| H-7 | N+1 confirmado: `lease.service.ts:186–216` e `recurring.service.ts:109–149` (loop de findFirst+create/update em cron jobs) | `modules/property-management/` + `modules/finance/` |
| H-8 | `prisma-soft-delete.ts` — arquivo inteiro com zero importadores. Dead code completo | `apps/api/src/middleware/prisma-soft-delete.ts` |
| H-9 | Migrations `ADD COLUMN` sem `IF NOT EXISTS` na maioria dos arquivos gerados — reexecução por erro operacional causa falha | `migrations/20260418030000`, `20260418040000`, outros |
| H-10 | `kaven-ui-base/.gitignore` sem `*.env`/`.env*` — repo público, risco de commit acidental de tokens | `kaven-ui-base/.gitignore` |

---

## 🟡 MEDIUM

| # | Finding | Arquivo |
|---|---------|---------|
| M-1 | `encryption.util.ts:17` — `ENCRYPTION_KEY \|\| JWT_SECRET` compartilha material de chave | `apps/api/src/utils/encryption.util.ts` |
| M-2 | `refreshToken` gerado com `Math.random()` — não criptograficamente seguro | `apps/api/src/lib/jwt.ts:32` |
| M-3 | Prisma 5.22.0 — major lag (Prisma 6.x+). Sem query batching e edge runtime melhorado | `package.json` (root + workspaces) |
| M-4 | TypeScript versão conflitante: `^6.0.2` root vs `^5.3.3` em workspaces | múltiplos `package.json` |
| M-5 | 3 email providers em `dependencies` de prod (`nodemailer`, `postmark`, `resend`) — apenas `resend` ativo | `apps/api/package.json` |
| M-6 | `app.ts` — 182 `register()` calls no startup. Cold starts no Cloud Run penalizados | `apps/api/src/app.ts` |
| M-7 | Sem coverage threshold no Vitest — testes unitários sem gate de cobertura mínima | `apps/api/vitest.config.ts` |
| M-8 | CI sem `timeout-minutes` — test hang pode bloquear runner por 6h | `.github/workflows/ci.yml` |
| M-9 | `checkout`, `billing-accounts`, `usage` — estrutura flat vs. padrão `controllers/services/` dos outros 43 módulos | `apps/api/src/modules/` |
| M-10 | `requireCapability` usado em apenas 5 de 43+ módulos — enforcement de capabilities inconsistente | `apps/api/src/modules/` |
| M-11 | FK `contact_id` e `manager_id` na tabela `projects` sem `@@index` | migration `20260325000000` |

---

## 🔵 LOW

| # | Finding |
|---|---------|
| L-1 | `ses.provider.ts` (868 linhas) — operacionalmente morto (Resend é o provider ativo) |
| L-2 | `storage.service.ts:81,109` — presigned URLs são stubs. Feature de documentos **quebrada em prod** |
| L-3 | `app.ts` (712 linhas) — registration manual sem auto-discovery |
| L-4 | `seed.ts:209` — `domain: "admin.kaven.dev"` hardcoded |
| L-5 | 3 branches remotas órfãs: `feat/f2.4.4b-webhook-hardening`, `feat/f2.4.5-drop-legacy-stripe-columns`, `fix/kaven-payments-module-injection-schema` |

---

## Placar Geral

| Severidade | Qtd |
|-----------|-----|
| 🔴 CRITICAL | 2 |
| 🟠 HIGH | 10 |
| 🟡 MEDIUM | 11 |
| 🔵 LOW | 5 |
| **Total** | **28** |

---

## Sequência de Ataque

### P0 — Hoje (segurança)
1. **C-1** — remover/guardar com `NODE_ENV === 'development'` o log do `jwt.ts:41`
2. **C-2** — mover `STRIPE_SECRET_KEY` para `env.ts`, eliminar acesso direto no monitor
3. **H-3** — corrigir `encryption.util.decrypt()` para propagar erro
4. **H-10** — `kaven-ui-base/.gitignore`: adicionar `*.env`, `.env*`, `.env.local`

### P1 — Esta semana (estabilidade)
5. **H-1** — `authorization.service.ts`: trocar `new PrismaClient()` pelo singleton `lib/prisma`
6. **H-5** — unskip as 5 suites GDPR/multi-tenant ou documentar motivo formal
7. **H-7** — refatorar N+1 em `lease.service` e `recurring.service`
8. **H-6** — unificar em `bcryptjs`, remover `bcrypt`
9. **H-8** — deletar `prisma-soft-delete.ts` (zero importadores)

### P2 — Próximas sprints
10. **H-2** — migrar `process.env.*` espalhados para `env.ts` (audit completo de call-sites)
11. **M-3** — Prisma 5 → 7 (sprint dedicada com breaking changes)
12. **L-2** — implementar presigned URLs reais (módulo de documentos quebrado em prod)
13. **M-2** — `crypto.randomBytes(32)` para `refreshToken`
14. **M-1** — `ENCRYPTION_KEY` obrigatória, sem fallback para `JWT_SECRET`
