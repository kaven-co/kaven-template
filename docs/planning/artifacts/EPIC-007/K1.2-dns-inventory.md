# K1.2 — DNS Inventory (Cloudflare · kaven.site)

## Objetivo

Listar todos os registros DNS da zona `kaven.site` e mapear o que muda na migração do `kaven-site` para produção.

**Regra:** não incluir tokens, segredos, nem prints com dados sensíveis.

---

## Metadados

| Campo | Valor |
|---|---|
| Data/Hora (UTC) | 2026-04-17 11:04:58 UTC |
| Executor (owner) | bychrisr |
| Revisor (4-eyes) | (pendente) |
| Zona | `kaven.site` |
| Ferramenta | `dig` (consulta pública) + Cloudflare Dashboard/API (para proxy e registros não-publicáveis) |

---

## Tabela de registros

| Nome | Tipo | Proxy | Destino/Valor (não-secreto) | Mantém? | Muda para | Notes |
|---|---|---|---|---|---|---|
| admin | CNAME | (preencher no Cloudflare) | `2a1ab9688dfbc9fc.vercel-dns-017.com.` | manter | — | Confirma Vercel DNS. |
| tenant | CNAME | (preencher no Cloudflare) | `61e29ae4d20cf40d.vercel-dns-017.com.` | manter | — | Confirma Vercel DNS. |
| marketplace | CNAME | (preencher no Cloudflare) | `ghs.googlehosted.com.` | manter | — | Confirma Google hosted (possível mapeamento p/ Cloud Run custom domain). |
| api | CNAME | — | (sem registro público detectado via `dig`) | decidir | (se criar) | Se for introduzir `api.kaven.site`, definir estratégia e apontar. |
| @ | A | (preencher no Cloudflare) | `216.198.79.1` | manter | — | IP atual da raiz `kaven.site`. |
| www | CNAME | — | (sem registro público detectado via `dig`) | decidir | (se criar) | Se existir no Cloudflare, preencher manualmente. |

---

## Evidências

- [ ] Inventário completo preenchido
- [ ] Registros de email (SPF/DKIM/DMARC) identificados
- [ ] Mapa “mantém vs muda” fechado com reviewer

