---
story_id: D1.10
title: Publish Payments Module — marketplace.kaven.site
epic: EPIC-006
sprint: sprint-d1
status: done
assignee: "@dev"
priority: P1
effort_hours: 8
completed: 2026-04-10
---

# D1.10 — Publish Payments Module — marketplace.kaven.site

> **Epic:** 6 (Deploy & Launch) | **Sprint:** D1 | **Agent:** @dev (Bolt)
> **Priority:** P1 — Primeiro módulo no marketplace, prova que o sistema funciona
> **Depends on:** D1.6 (Marketplace API deployed), D1.5 (Ed25519 keys para signing)

---

## Status (2026-04-10) — ✅ DONE

**✅ COMPLETO — payments@1.0.1 publicado no marketplace.**

**O que foi feito:**
- [x] `modules/kaven-payments/` criado com estrutura completa (backend + frontend + injections)
- [x] `module.json` com metadata, injections markers, files map, prisma models
- [x] Código extraído: routes (billing, payments, subscriptions, webhooks/stripe, webhooks/paddle), services (payment, subscription, entitlement, usage-tracking, stripe-client, paddle-client), prisma schema (Subscription, Invoice, Payment models), frontend (UpgradeButton, lib/paddle.ts)
- [x] Tests: 148 testes passando (billing/checkout/subscriptions)
- [x] Módulo empacotado: 16.4 KB
- [x] Checksum SHA-256 ✅
- [x] Assinatura Ed25519 ✅
- [x] Upload S3 ✅
- [x] Release criado no marketplace: payments@1.0.1 ✅
- [x] Registry restaurado para marketplace.kaven.site ✅
- [x] Husky removido (`package.json` prepare script + `.husky-removed/`)
- [x] Auth token gerado em `~/.kaven/auth.json` (JWT válido até 2027-04-09, gerado via JWT_SECRET local)
- [x] Marketplace LIVE no kaven-prod (kaventhecreator@gmail.com) — health 200 ✅

**Evidências:**
- PR #90: https://github.com/kaven-co/kaven-framework/pull/90 (CI verde, mergeado)
- Release: payments@1.0.1 no marketplace.kaven.site
- S3: upload + Ed25519 signature verificada
- Marketplace URL: https://kaven-marketplace-1096722897164.us-east1.run.app (health 200 ✅)

---

## Contexto

Para provar que o sistema de marketplace funciona end-to-end, precisamos publicar o primeiro módulo real. O módulo `payments` é perfeito para isso porque:
- Já existe no framework (apps/api/src/modules/billing/ + checkout/)
- É um módulo valioso que justifica o marketplace
- Tem schema Prisma, rotas, e serviços definidos
- Exercita toda a pipeline: extract → module.json → kaven module publish → S3 → Ed25519 sign

## User Story

**Como** dev do Kaven,
**Quero** extrair o módulo payments do framework e publicá-lo no marketplace,
**Para que** usuários possam instalar via `kaven module install payments`.

## Acceptance Criteria

- [x] Diretório `modules/payments/` criado na raiz do kaven-framework
- [x] `module.json` criado com metadata completo
- [x] Código do módulo extraído: prisma schema extension, routes, services, tests
- [x] `kaven module publish --dry-run` passa sem erros
- [x] `kaven module publish` faz upload para S3 e assina com Ed25519
- [x] Release payments@1.0.1 criado no marketplace
- [x] Registry restaurado para marketplace.kaven.site

## Technical Notes

**Estrutura do módulo payments:**
```
kaven-framework/modules/payments/
├── module.json                    ← Metadata
├── prisma/
│   └── payments.prisma            ← Schema extension (Stripe, Paddle tables)
├── src/
│   ├── routes/
│   │   ├── checkout.routes.ts
│   │   └── billing.routes.ts
│   ├── services/
│   │   ├── stripe.service.ts
│   │   └── paddle.service.ts
│   └── index.ts                   ← Module entry point
├── tests/
│   └── payments.spec.ts
└── README.md                      ← Como instalar + configurar
```

**module.json:**
```json
{
  "id": "payments",
  "name": "Kaven Payments",
  "version": "1.0.0",
  "description": "Multi-provider payment integration — Stripe + Paddle + PIX",
  "tier": "pro",
  "category": "payments",
  "author": "kaven-co",
  "license": "commercial",
  "kaven": ">=1.0.0-rc1",
  "dependencies": [],
  "scripts": {
    "postinstall": "npx prisma db push"
  },
  "env": [
    "STRIPE_SECRET_KEY",
    "STRIPE_WEBHOOK_SECRET",
    "PADDLE_API_KEY",
    "PADDLE_WEBHOOK_SECRET"
  ],
  "files": [
    "prisma/",
    "src/",
    "tests/",
    "README.md"
  ]
}
```

**Publicar:**
```bash
cd kaven-framework/modules/payments

# Dry run — validar sem publicar
kaven module publish --dry-run
# Deve mostrar: ✓ module.json válido, ✓ arquivos encontrados, ✓ signing key OK

# Publicar
kaven module publish
# Deve mostrar: ✓ uploading... ✓ signed with Ed25519 ✓ published to marketplace
```

**Verificar no marketplace:**
```bash
# Listar módulos
curl https://marketplace.kaven.site/modules | jq '.[] | {id, name, version}'

# Instalar em projeto de teste
cd /tmp/test-project
kaven module install payments
```

**Checar compatibilidade de placeholders no code:**
O módulo publicado tem `[KAVEN_MODULE:payments BEGIN]` e `[KAVEN_MODULE:payments END]` markers para idempotency do CLI.

## Definition of Done

- [x] modules/kaven-payments/ criado com estrutura completa
- [x] module.json válido (kaven module publish --dry-run passou)
- [x] Upload para S3 bem-sucedido com assinatura Ed25519
- [x] Release payments@1.0.1 criado no marketplace
- [x] Registry restaurado para marketplace.kaven.site

## Time Estimate

- Extrair código de billing/checkout do framework: 3h
- Criar module.json + estrutura: 1h
- Dry run + ajustes: 1h
- Publish real + verificar S3: 1h
- Testar install em projeto de teste: 2h
- **Total: 8h**
