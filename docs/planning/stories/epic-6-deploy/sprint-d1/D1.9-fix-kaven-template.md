---
story_id: D1.9
title: Fix kaven-template — Framework Snapshot com Placeholders
epic: EPIC-006
sprint: sprint-d1
status: done
assignee: "@dev"
priority: P0
effort_hours: 16
---

# D1.9 — Fix kaven-template — Framework Snapshot com Placeholders

> **Epic:** 6 (Deploy & Launch) | **Sprint:** D1 | **Agent:** @dev (Bolt) + @architect (Atlas)
> **Priority:** P0 — BLOCKER #0 — sem isso, `kaven init` não funciona
> **Depends on:** D1.5 (SIGNING_PUBLIC_KEY para hardcode no CLI)

---

## Contexto

O `kaven-co/kaven-template` atual é um repositório genérico vazio que não tem a estrutura do framework. O `kaven init` espera clonar um template que:
- Tem `.env.example` com `{{PLACEHOLDERS}}`
- Tem `prisma/schema.prisma` completo
- Tem a estrutura monorepo completa: `apps/api`, `apps/admin`, `apps/tenant`, `packages/`

**Decisão arquitetural:** O template será um snapshot limpo do kaven-framework (sem histórico git, sem dados sensíveis, com placeholders onde o CLI faz substituição via EnvManager).

Esta é a story mais complexa do Sprint D1 — exige coordenação entre @dev (implementação) e @architect (decisões de estrutura do template).

## User Story

**Como** desenvolvedor usando o Kaven,
**Quero** que `kaven init my-saas` clone um template completo e funcional do framework,
**Para que** eu possa começar a desenvolver meu SaaS sem configurar infraestrutura do zero.

## Acceptance Criteria

- [ ] Template criado como snapshot limpo do kaven-framework (sem .git, node_modules, .aios-core, squads, lp)
- [ ] `.env.example` criado com todos os placeholders necessários (ver Technical Notes)
- [ ] `prisma/schema.prisma` incluído e funcional
- [ ] `apps/api/package.json` incluído com `{{PROJECT_NAME}}` como placeholder
- [ ] `apps/admin` e `apps/tenant` incluídos com estrutura completa
- [ ] `packages/ui`, `packages/database`, `packages/shared` incluídos
- [ ] `turbo.json`, `pnpm-workspace.yaml`, `tsconfig.json` na raiz
- [ ] `kaven init my-saas` executa sem erros de estrutura faltando
- [ ] Placeholders `{{PROJECT_NAME}}`, `{{DATABASE_URL}}`, `{{DEFAULT_LOCALE}}`, `{{DEFAULT_CURRENCY}}` substituídos corretamente pelo CLI
- [ ] Template pushed para `kaven-co/kaven-template` no GitHub
- [ ] `kaven init` documentado em README.md do template

## Technical Notes

**Estrutura esperada do template:**
```
kaven-template/
├── .env.example              ← COM PLACEHOLDERS
├── .gitignore
├── package.json              ← COM {{PROJECT_NAME}}
├── turbo.json
├── pnpm-workspace.yaml
├── tsconfig.json
├── apps/
│   ├── api/
│   │   ├── package.json      ← COM {{PROJECT_NAME}}
│   │   ├── src/              ← Framework API source completo
│   │   └── tsconfig.json
│   ├── admin/
│   │   ├── package.json
│   │   ├── src/
│   │   └── next.config.ts
│   └── tenant/
│       ├── package.json
│       ├── src/
│       └── next.config.ts
├── packages/
│   ├── database/
│   │   ├── prisma/
│   │   │   └── schema.prisma ← Schema base completo
│   │   └── package.json
│   ├── ui/                   ← @kaven/ui-base
│   └── shared/               ← @kaven/shared
└── docs/
    └── README.md
```

**.env.example com placeholders:**
```bash
# ===== KAVEN FRAMEWORK CONFIGURATION =====
# Gerado por: kaven init {{PROJECT_NAME}}

# Project
PROJECT_NAME={{PROJECT_NAME}}
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL={{DATABASE_URL}}

# Authentication
JWT_SECRET=change-me-in-production
REFRESH_TOKEN_SECRET=change-me-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Locale & Currency
DEFAULT_LOCALE={{DEFAULT_LOCALE}}
DEFAULT_CURRENCY={{DEFAULT_CURRENCY}}

# Email — Option 1: AWS SES (recomendado)
AWS_SES_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
SES_FROM_EMAIL=noreply@yourdomain.com

# Email — Option 2: SMTP
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=

# Redis (para rate limiting e cache)
REDIS_URL=redis://localhost:6379

# Kaven Marketplace (para instalar módulos)
MARKETPLACE_URL=https://marketplace.kaven.site
KAVEN_LICENSE_KEY=

# Payments (Stripe — opcional, installable module)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
```

**Script de criação do snapshot:**
```bash
#!/bin/bash
# Criar template a partir do framework atual

# 1. Copiar framework
cp -r kaven-framework/ kaven-template-new/

# 2. Entrar no diretório
cd kaven-template-new

# 3. Remover o que NÃO deve ir para o template
rm -rf .git
rm -rf node_modules
rm -rf .aios-core
rm -rf squads
rm -rf lp
rm -rf docs/planning
rm -rf docs/sessions
rm -rf _business
rm -rf .claude
rm -rf dist .next
rm -rf apps/api/dist
rm -rf coverage
find . -name "*.pyc" -delete
find . -name "__pycache__" -delete

# 4. Substituir project name nos arquivos
find . -name "*.json" -exec sed -i 's/kaven-framework/{{PROJECT_NAME}}/g' {} \;
find . -name "*.json" -exec sed -i 's/@kaven\/framework/{{PROJECT_NAME}}/g' {} \;

# 5. Criar .env.example com placeholders
# (conteúdo definido acima)

# 6. Renomear e push
git init
git add .
git commit -m "chore: initial kaven-template snapshot from v1.0.0-rc1"
git remote add origin git@github.com:kaven-co/kaven-template.git
git push --force origin main
```

**Verificar alinhamento CLI → Template:**
Verificar em `kaven-cli/src/commands/init/index.ts` quais placeholders o CLI substitui:
- O EnvManager deve substituir: `{{PROJECT_NAME}}`, `{{DATABASE_URL}}`, `{{DEFAULT_LOCALE}}`, `{{DEFAULT_CURRENCY}}`
- Se CLI usa outros placeholders, template deve ter exatamente os mesmos

## Definition of Done

- [ ] Template criado como snapshot do framework v1.0.0-rc1
- [ ] .env.example com todos os placeholders do CLI
- [ ] `kaven init test-project` executa sem erros (teste local)
- [ ] Template pushed para kaven-co/kaven-template
- [ ] README.md do template atualizado com instruções de uso
- [ ] BLOCKER #0 resolvido ✅

## Time Estimate

- Criar script de snapshot: 2h
- Executar + limpar template: 2h
- Criar .env.example com todos os placeholders: 2h
- Alinhar placeholders com CLI EnvManager: 2h
- Testar `kaven init` end-to-end local: 4h
- Debug e ajustes: 2h
- Push para GitHub: 30min
- Atualizar README: 1h
- **Total: 16h** (story mais complexa do sprint — requer coordenação @dev + @architect)
