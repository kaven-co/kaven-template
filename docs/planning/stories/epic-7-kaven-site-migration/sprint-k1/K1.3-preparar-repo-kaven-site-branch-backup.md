---
story_id: K1.3
title: Preparar repo kaven-site_ (backup S1 + branch strategy + branch limpo)
epic: EPIC-007
sprint: sprint-k1
status: in_progress
assignee: "@devops"
priority: P0
effort_hours: 3
---

# K1.3 — Preparar repo kaven-site_ (backup S1 + branch strategy + branch limpo)

> **Epic:** 7 (Kaven Site Production Migration) | **Sprint:** K1 | **Agent:** @devops (Deploy)  
> **Priority:** P0 — Sem branch limpo, `kaven init` vira “migração manual” e quebra dogfooding.  
> **Depends on:** Nenhum

---

## Contexto

O repo `kaven-site_` já possui trabalho anterior (Sprint S1) que **não deve ser destruído**, mas a migração exige um estado **limpo** para rodar `kaven init` exatamente como um cliente faria.

---

## User Story

**Como** executor da migração,  
**Quero** preservar o histórico útil e criar um branch limpo para bootstrap,  
**Para que** eu consiga executar `kaven init` com rastreabilidade e sem risco de perda.

---

## Acceptance Criteria

- [ ] Branch `feat/sprint-s1-landing` verificado e preservado (hash/commit anotado).
- [ ] Backup local/branch de referência do S1 criado (sem apagar nada).
- [ ] Estratégia definida e documentada:
  - branch “limpo” a partir de `main` (ou outra base) **sem** deletar S1
  - naming + regras de merge
- [ ] Branch limpo pronto para receber o `kaven init` (sem arquivos conflitantes).
- [ ] Checklist “não destruir S1” concluído.

---

## Technical Notes

### Artefato obrigatório
- `docs/planning/artifacts/EPIC-007/K1.3-branch-strategy.md`

### Regras
- Proibido apagar branch S1.
- Proibido reescrever histórico público sem necessidade.

---

## Evidence (obrigatória)

- [ ] Hash do branch S1 e do backup documentados.
- [ ] Nome do branch limpo e base commit documentados.

---

## Definition of Done

- [ ] S1 preservado com referência clara.
- [ ] Branch limpo criado e pronto para o bootstrap.

---

## Time Estimate

- Inspeção + backup: 1h
- Branch strategy + docs: 1h
- Criação e validação do branch limpo: 1h
- **Total: 3h**

---

## Execution Log

- 2026-04-16: Bloqueio detectado neste host: o caminho esperado do repo `kaven-site_` não existe (`/home/bychrisr/projects/work/kaven/kaven-site_`).
  - Ação necessária pra destravar: clonar/baixar o repo `kaven-site` (ou informar o path correto) e então executar backup/branch strategy.
- 2026-04-17: Repo novo criado localmente em `/home/bychrisr/projects/work/kaven/kaven-site_` e `git init` executado (sem commits). Próximo passo desta story: decidir estratégia de branch baseline (main vazia) e preservar referência do Sprint S1 via backup externo (não aplicável no repo novo).

