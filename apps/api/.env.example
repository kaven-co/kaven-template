# Server Configuration
PORT=8000
NODE_ENV=development

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/kaven_db?schema=public"

# Redis (Queue & Cache)
REDIS_URL="redis://localhost:6379"

# Security
JWT_SECRET="changeme_super_secret_key"
JWT_EXPIRES_IN="7d"

# Frontend Integration
CORS_ORIGIN="http://localhost:3000,http://localhost:3001,http://localhost:3002"
FRONTEND_URL="http://localhost:3000"

# Mailer
SMTP_HOST="localhost"
SMTP_PORT=1025
SMTP_USER="user"
SMTP_PASS="password"
SMTP_FROM="noreply@kaven.dev"

# Stripe (Optional)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Image Remote Patterns (Optional - used by admin/tenant Next.js apps)
# Comma-separated list of allowed image hostnames for next/image optimization
# Defaults to: flagcdn.com,api.dicebear.com
# IMAGE_REMOTE_HOSTNAMES=flagcdn.com,api.dicebear.com,cdn.example.com
