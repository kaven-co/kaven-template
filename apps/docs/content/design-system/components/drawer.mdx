---
title: Drawer
description: Painéis laterais deslizantes para navegação e conteúdo
---

import {
  Drawer,
  DrawerContent,
  DrawerHeader,
  DrawerTitle,
  DrawerTrigger,
} from '@kaven/ui-base';
import { Button } from '@kaven/ui-base';
import Link from 'next/link';
import { ComponentPreview } from '@/components/ComponentPreview';

# Drawer

> Painel lateral que desliza sobre o conteúdo para exibir navegação ou informações adicionais.

## Variações

### Básico

```tsx
import {
  Drawer,
  DrawerContent,
  DrawerHeader,
  DrawerTitle,
} from '@kaven/ui-base';

function BasicDrawer() {
  const [open, setOpen] = useState(false);

  return (
    <>
      <Button onClick={() => setOpen(true)}>Abrir Drawer</Button>

      <Drawer open={open} onOpenChange={setOpen}>
        <DrawerContent>
          <DrawerHeader>
            <DrawerTitle>Título do Drawer</DrawerTitle>
          </DrawerHeader>
          <div className="p-4">Conteúdo do drawer</div>
        </DrawerContent>
      </Drawer>
    </>
  );
}
```

### Posições

```tsx
// Left (padrão)
<Drawer side="left">...</Drawer>

// Right
<Drawer side="right">...</Drawer>

// Top
<Drawer side="top">...</Drawer>

// Bottom
<Drawer side="bottom">...</Drawer>
```

### Com Navegação

<ComponentPreview>
  <div className="flex w-full gap-4 flex-wrap justify-center items-center">
    <Drawer>
      <DrawerTrigger asChild>
        <Button variant="outline">Abrir Menu</Button>
      </DrawerTrigger>
      <DrawerContent className="w-80">
        <DrawerHeader>
          <DrawerTitle>Menu</DrawerTitle>
        </DrawerHeader>
        <nav className="p-4 space-y-2">
          <Link href="/dashboard" className="block p-2 hover:bg-muted rounded">
            Dashboard
          </Link>
          <Link href="/users" className="block p-2 hover:bg-muted rounded">
            Usuários
          </Link>
          <Link href="/settings" className="block p-2 hover:bg-muted rounded">
            Configurações
          </Link>
        </nav>
      </DrawerContent>
    </Drawer>
  </div>
</ComponentPreview>

```tsx
<Drawer open={open} onOpenChange={setOpen}>
  <DrawerContent className="w-80">
    <DrawerHeader>
      <DrawerTitle>Menu</DrawerTitle>
    </DrawerHeader>
    <nav className="p-4 space-y-2">
      <Link href="/dashboard" className="block p-2 hover:bg-muted rounded">
        Dashboard
      </Link>
      <Link href="/users" className="block p-2 hover:bg-muted rounded">
        Usuários
      </Link>
      <Link href="/settings" className="block p-2 hover:bg-muted rounded">
        Configurações
      </Link>
    </nav>
  </DrawerContent>
</Drawer>
```

## API

| Prop           | Tipo                                     | Padrão   | Descrição             |
| -------------- | ---------------------------------------- | -------- | --------------------- |
| `open`         | `boolean`                                | -        | Controla visibilidade |
| `onOpenChange` | `(open: boolean) => void`                | -        | Callback quando muda  |
| `side`         | `'left' \| 'right' \| 'top' \| 'bottom'` | `'left'` | Posição do drawer     |

## Relacionados

- [Dialog](/design-system/components/dialog) - Modais centralizados
- [Navbar](/design-system/components/navbar) - Navegação principal
