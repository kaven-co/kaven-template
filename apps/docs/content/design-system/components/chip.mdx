---
title: Chip
description: Chips removíveis para tags e filtros
---

import { Chip } from '@kaven/ui-base';
import { ComponentPreview } from '@/components/ComponentPreview';

# Chip

> Pequenos elementos para exibir tags, filtros ou seleções removíveis.

## Básico

```tsx
import { Chip } from '@kaven/ui-base';

<Chip>Tag</Chip>;
```

## Com Ícone

```tsx
import { X } from 'lucide-react';

<Chip onDelete={() => handleDelete()}>
  Tag Removível
  <X className="h-3 w-3 ml-1" />
</Chip>;
```

## Cores

<ComponentPreview>
  <div className="flex w-full gap-4 flex-wrap justify-center items-center">
    <Chip color="primary">Primary</Chip>
    <Chip color="success">Success</Chip>
    <Chip color="error">Error</Chip>
  </div>
</ComponentPreview>

```tsx
<Chip color="primary">Primary</Chip>
<Chip color="success">Success</Chip>
<Chip color="error">Error</Chip>
```

## Variantes

<ComponentPreview>
  <div className="flex w-full gap-4 flex-wrap justify-center items-center">
    <Chip variant="filled">Filled</Chip>
    <Chip variant="outlined">Outlined</Chip>
  </div>
</ComponentPreview>

```tsx
<Chip variant="filled">Filled</Chip>
<Chip variant="outlined">Outlined</Chip>
```

## Grupo de Chips

<ComponentPreview>
  <div className="flex w-full gap-4 flex-wrap justify-center items-center">
    <div className="flex flex-wrap gap-2">
      <Chip>React</Chip>
      <Chip>Next.js</Chip>
      <Chip>Tailwind</Chip>
    </div>
  </div>
</ComponentPreview>

```tsx
<div className="flex flex-wrap gap-2">
  {tags.map((tag) => (
    <Chip key={tag} onDelete={() => removeTag(tag)}>
      {tag}
    </Chip>
  ))}
</div>
```

## API

| Prop       | Tipo                                | Padrão      | Descrição           |
| ---------- | ----------------------------------- | ----------- | ------------------- |
| `variant`  | `'filled' \| 'outlined'`            | `'filled'`  | Estilo              |
| `color`    | `'primary' \| 'success' \| 'error'` | `'primary'` | Cor                 |
| `onDelete` | `() => void`                        | -           | Callback ao remover |

## Relacionados

- [Badge](/design-system/components/badge)
