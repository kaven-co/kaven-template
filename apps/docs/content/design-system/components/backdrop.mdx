---
title: Backdrop
description: Overlay de fundo para modais
---

import { Backdrop } from '@kaven/ui-base';
import { ComponentPreview } from '@/components/ComponentPreview';

# Backdrop

> Overlay escuro de fundo usado em modais e drawers.

## Básico

```tsx
import { Backdrop } from '@kaven/ui-base';

<Backdrop open={open} onClick={() => setOpen(false)} />;
```

## Com Conteúdo

<ComponentPreview>
  <div className="flex w-full gap-4 flex-wrap justify-center items-center">
    <div className="text-center p-4 border border-dashed rounded">
      Backdrop Preview (Set open=true to see)
      <Backdrop open={false}>
        <div className="bg-card p-6 rounded-lg">
          <h2>Modal Content</h2>
        </div>
      </Backdrop>
    </div>
  </div>
</ComponentPreview>

```tsx
<Backdrop open={open}>
  <div className="bg-card p-6 rounded-lg" onClick={(e) => e.stopPropagation()}>
    <h2>Modal Content</h2>
  </div>
</Backdrop>
```

## API

| Prop        | Tipo         | Padrão  | Descrição          |
| ----------- | ------------ | ------- | ------------------ |
| `open`      | `boolean`    | `false` | Visibilidade       |
| `onClick`   | `() => void` | -       | Callback ao clicar |
| `invisible` | `boolean`    | `false` | Backdrop invisível |

## Relacionados

- [Dialog](/design-system/components/dialog)
- [Drawer](/design-system/components/drawer)
