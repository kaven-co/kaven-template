---
title: Email DNS Setup
description: Guia completo de configuração DNS para alta entregabilidade de emails com SPF, DKIM, DMARC, MTA-STS e BIMI.
date: 2026-01-19
author: Chris + Antigravity
version: 2.0.0
tags: [email, dns, spf, dkim, dmarc, bimi, deliverability, cloudflare]
---

# Email DNS Setup

> Configuração completa de DNS para garantir 83%+ de inbox placement e conformidade com padrões de autenticação.

## Visão Geral

Este guia detalha a configuração DNS necessária para alta entregabilidade de emails, incluindo autenticação SPF/DKIM/DMARC, isolamento de fluxos transacionais vs. marketing, e implementação de BIMI.

**Objetivos:**

- ✅ Configurar autenticação SPF/DKIM/DMARC
- ✅ Isolar fluxos transacionais vs. marketing
- ✅ Atingir score 10/10 no Mail Tester
- ✅ Prevenir spoofing e phishing
- ✅ Habilitar BIMI (Brand Indicators)

## Arquitetura de Subdomínios

### Por que usar subdomínios isolados?

**Benefícios:**

1. **Proteção de Reputação:** Spam complaints em marketing não afetam transacionais
2. **Políticas DMARC Diferentes:** `p=reject` em transacional, `p=quarantine` em marketing
3. **Warm-up Independente:** Cronogramas separados por tipo
4. **Monitoramento Granular:** Métricas isoladas

### Estrutura Proposta

```
kaven.com (domínio principal)
├── auth.kaven.com → E-mails Transacionais
│   ├── Welcome emails
│   ├── Password reset
│   ├── Email verification
│   ├── Invoices
│   └── Security alerts
│
└── mail.kaven.com → E-mails de Marketing
    ├── Newsletters
    ├── Product updates
    ├── Lifecycle campaigns
    └── Announcements
```

## Configuração no Cloudflare

### Passo 1: Acessar DNS Management

1. Login no Cloudflare: [dash.cloudflare.com](https://dash.cloudflare.com/)
2. Selecione o domínio `kaven.com`
3. Navegue para **DNS** > **Records**

### Passo 2: Adicionar Registros DNS

#### Tabela de Registros Completa

| Tipo    | Nome                 | Conteúdo                                  | TTL  | Proxy    | Notas         |
| ------- | -------------------- | ----------------------------------------- | ---- | -------- | ------------- |
| **A**   | `auth`               | IP do servidor ou CNAME                   | 3600 | DNS only | Transacional  |
| **A**   | `mail`               | IP do servidor ou CNAME                   | 3600 | DNS only | Marketing     |
| **MX**  | `@`                  | `10 mail.kaven.com`                       | 3600 | DNS only | Mail exchange |
| **TXT** | `@`                  | `v=spf1 include:spf.postmarkapp.com ~all` | 3600 | DNS only | SPF           |
| **TXT** | `_dmarc`             | Ver abaixo                                | 3600 | DNS only | DMARC         |
| **TXT** | `pm._domainkey.auth` | Fornecido pelo provider                   | 3600 | DNS only | DKIM auth     |
| **TXT** | `pm._domainkey.mail` | Fornecido pelo provider                   | 3600 | DNS only | DKIM mail     |
| **TXT** | `_mta-sts`           | `v=STSv1; id=TIMESTAMP`                   | 3600 | DNS only | TLS           |
| **TXT** | `default._bimi`      | `v=BIMI1; l=https://kaven.com/logo.svg`   | 3600 | DNS only | Brand logo    |

## Detalhamento dos Registros

### 1. SPF (Sender Policy Framework)

**Registro TXT para `@` (domínio raiz):**

```dns
v=spf1 include:spf.postmarkapp.com ~all
```

**Explicação:**

- `v=spf1`: Versão do SPF
- `include:spf.postmarkapp.com`: Autoriza servidores do Postmark
- `~all`: Soft fail (mudar para `-all` após validação)

**Provedores Alternativos:**

- **Resend:** `v=spf1 include:_spf.resend.com ~all`
- **SendGrid:** `v=spf1 include:sendgrid.net ~all`
- **AWS SES:** `v=spf1 include:amazonses.com ~all`

> [!WARNING]
> SPF tem limite de 10 lookups DNS. Use [SPF Flattener](https://dmarcian.com/spf-survey/) se necessário.

---

### 2. DKIM (DomainKeys Identified Mail)

**Registros TXT para subdomínios:**

```dns
Nome: pm._domainkey.auth.kaven.com
Valor: [FORNECIDO PELO PROVEDOR]

Nome: pm._domainkey.mail.kaven.com
Valor: [FORNECIDO PELO PROVEDOR]
```

**Como obter a chave DKIM:**

#### Postmark

1. Acesse [account.postmarkapp.com/servers](https://account.postmarkapp.com/servers)
2. Clique no servidor → **Settings** → **Sender Signatures**
3. Adicione `auth.kaven.com` e `mail.kaven.com`
4. Copie os valores DKIM fornecidos

#### Resend

1. Acesse [resend.com/domains](https://resend.com/domains)
2. Clique em **Add Domain**
3. Adicione `auth.kaven.com` e `mail.kaven.com`
4. Copie os registros DKIM

**Exemplo de valor DKIM:**

```
k=rsa; p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC...
```

---

### 3. DMARC (Domain-based Message Authentication)

**Registro TXT para `_dmarc.kaven.com`:**

#### Fase 1: Monitoramento (Primeiros 30 dias)

```dns
v=DMARC1; p=none; rua=mailto:dmarc@kaven.com; ruf=mailto:dmarc@kaven.com; aspf=s; adkim=s; pct=100; fo=1
```

#### Fase 2: Quarentena (Dias 31-60)

```dns
v=DMARC1; p=quarantine; rua=mailto:dmarc@kaven.com; ruf=mailto:dmarc@kaven.com; aspf=s; adkim=s; pct=100; fo=1
```

#### Fase 3: Rejeição (Após 60 dias - Estado Final)

```dns
v=DMARC1; p=reject; rua=mailto:dmarc@kaven.com; ruf=mailto:dmarc@kaven.com; aspf=s; adkim=s; pct=100; fo=1
```

**Parâmetros:**

- `v=DMARC1`: Versão
- `p=none|quarantine|reject`: Política de enforcement
- `rua=`: Email para relatórios agregados (diários)
- `ruf=`: Email para relatórios forenses (falhas individuais)
- `aspf=s`: SPF strict alignment
- `adkim=s`: DKIM strict alignment
- `pct=100`: Aplicar em 100% dos emails
- `fo=1`: Gerar relatório se SPF ou DKIM falhar

> [!TIP]
> Use [dmarcian.com](https://dmarcian.com/) ou [dmarc.postmarkapp.com](https://dmarc.postmarkapp.com/) para análise de relatórios.

---

### 4. MTA-STS (SMTP TLS Reporting)

**Registro TXT para `_mta-sts.kaven.com`:**

```dns
v=STSv1; id=20260119000000
```

**Arquivo de política (hospedar em `https://mta-sts.kaven.com/.well-known/mta-sts.txt`):**

```txt
version: STSv1
mode: enforce
mx: mail.kaven.com
max_age: 604800
```

**Benefício:** Força uso de TLS em conexões SMTP, prevenindo downgrade attacks.

---

### 5. BIMI (Brand Indicators for Message Identification)

**Registro TXT para `default._bimi.kaven.com`:**

```dns
v=BIMI1; l=https://kaven.com/logo.svg; a=
```

**Requisitos:**

- Logo em formato SVG Tiny PS
- DMARC com `p=quarantine` ou `p=reject`
- Certificado VMC (opcional mas recomendado)

**Onde aparece:**

- Gmail (desktop e mobile)
- Yahoo Mail
- Apple Mail (iOS 16+)

> [!IMPORTANT]
> Valide seu logo em [bimigroup.org/bimi-generator](https://bimigroup.org/bimi-generator/)

## Checklist de Validação

### Após Configurar DNS (aguardar 24-48h)

- [ ] **SPF Validado**
  - Ferramenta: [mxtoolbox.com/spf.aspx](https://mxtoolbox.com/spf.aspx)
  - Resultado esperado: "SPF Record found" (verde)

- [ ] **DKIM Ativo**
  - Enviar email de teste
  - Ver headers (Gmail: "Show original")
  - Procurar por `dkim=pass`

- [ ] **DMARC Configurado**
  - Ferramenta: [mxtoolbox.com/dmarc.aspx](https://mxtoolbox.com/dmarc.aspx)
  - Resultado esperado: Registro válido

- [ ] **MX Records Resolvendo**

  ```bash
  dig MX kaven.com +short
  # Deve retornar: 10 mail.kaven.com
  ```

- [ ] **Subdomínios Resolvendo**
  ```bash
  dig auth.kaven.com +short
  dig mail.kaven.com +short
  # Devem retornar IPs ou CNAMEs
  ```

## Scripts de Validação

### Script Bash (Linux/Mac)

Salve como `validate-dns.sh`:

```bash
#!/bin/bash

DOMAIN="kaven.com"
AUTH_SUBDOMAIN="auth.kaven.com"
MAIL_SUBDOMAIN="mail.kaven.com"

echo "🔍 Validando DNS para $DOMAIN..."
echo ""

# SPF
echo "📧 SPF Record:"
dig TXT $DOMAIN +short | grep "v=spf1"
echo ""

# DKIM (auth)
echo "🔐 DKIM (auth subdomain):"
dig TXT pm._domainkey.$AUTH_SUBDOMAIN +short
echo ""

# DKIM (mail)
echo "🔐 DKIM (mail subdomain):"
dig TXT pm._domainkey.$MAIL_SUBDOMAIN +short
echo ""

# DMARC
echo "🛡️ DMARC Record:"
dig TXT _dmarc.$DOMAIN +short
echo ""

# MX
echo "📬 MX Records:"
dig MX $DOMAIN +short
echo ""

# Subdomains
echo "🌐 Auth Subdomain:"
dig A $AUTH_SUBDOMAIN +short
echo ""

echo "🌐 Mail Subdomain:"
dig A $MAIL_SUBDOMAIN +short
echo ""

echo "✅ Validação completa!"
```

**Executar:**

```bash
chmod +x validate-dns.sh
./validate-dns.sh
```

### Script Node.js

Criar em `apps/api/scripts/validate-email-dns.ts`:

```typescript
import { execSync } from 'child_process';

const DOMAIN = 'kaven.com';
const AUTH_SUBDOMAIN = 'auth.kaven.com';
const MAIL_SUBDOMAIN = 'mail.kaven.com';

interface ValidationResult {
  name: string;
  status: 'pass' | 'fail' | 'warning';
  message: string;
}

const results: ValidationResult[] = [];

function checkDNS(
  type: string,
  domain: string,
  expected?: string,
): ValidationResult {
  try {
    const output = execSync(`dig ${type} ${domain} +short`, {
      encoding: 'utf-8',
    });

    if (!output.trim()) {
      return {
        name: `${type} ${domain}`,
        status: 'fail',
        message: 'Nenhum registro encontrado',
      };
    }

    if (expected && !output.includes(expected)) {
      return {
        name: `${type} ${domain}`,
        status: 'warning',
        message: `Encontrado: ${output.trim()}, Esperado: ${expected}`,
      };
    }

    return {
      name: `${type} ${domain}`,
      status: 'pass',
      message: output.trim(),
    };
  } catch (error) {
    return {
      name: `${type} ${domain}`,
      status: 'fail',
      message: 'Erro ao verificar DNS',
    };
  }
}

console.log('🔍 Validando configuração DNS de email...\\n');

// SPF
results.push(checkDNS('TXT', DOMAIN, 'v=spf1'));

// DKIM
results.push(checkDNS('TXT', `pm._domainkey.${AUTH_SUBDOMAIN}`));
results.push(checkDNS('TXT', `pm._domainkey.${MAIL_SUBDOMAIN}`));

// DMARC
results.push(checkDNS('TXT', `_dmarc.${DOMAIN}`, 'v=DMARC1'));

// MX
results.push(checkDNS('MX', DOMAIN));

// Subdomains
results.push(checkDNS('A', AUTH_SUBDOMAIN));
results.push(checkDNS('A', MAIL_SUBDOMAIN));

// Print results
console.log('📊 Resultados:\\n');
results.forEach((result) => {
  const icon =
    result.status === 'pass' ? '✅' : result.status === 'warning' ? '⚠️' : '❌';
  console.log(`${icon} ${result.name}`);
  console.log(`   ${result.message}\\n`);
});

const failCount = results.filter((r) => r.status === 'fail').length;
const warnCount = results.filter((r) => r.status === 'warning').length;

console.log('\\n📈 Resumo:');
console.log(`   Passou: ${results.length - failCount - warnCount}`);
console.log(`   Avisos: ${warnCount}`);
console.log(`   Falhou: ${failCount}`);

if (failCount === 0 && warnCount === 0) {
  console.log('\\n🎉 Todos os registros DNS estão configurados corretamente!');
  process.exit(0);
} else {
  console.log('\\n⚠️ Alguns registros precisam de atenção.');
  process.exit(1);
}
```

**Executar:**

```bash
cd apps/api
pnpm tsx scripts/validate-email-dns.ts
```

## Troubleshooting

### Problema: SPF "Too Many DNS Lookups"

**Causa:** SPF tem limite de 10 lookups DNS.

**Solução:**

1. Use [SPF Flattener](https://dmarcian.com/spf-survey/)
2. Ou simplifique includes:
   ```dns
   v=spf1 ip4:192.0.2.1 ip4:192.0.2.2 ~all
   ```

---

### Problema: DKIM não validando

**Causas comuns:**

- Chave DKIM não propagada (aguardar 24-48h)
- Selector incorreto (deve ser `pm` para Postmark)
- Espaços extras no valor TXT

**Validação:**

```bash
dig TXT pm._domainkey.auth.kaven.com +short
```

---

### Problema: DMARC reports não chegando

**Causas:**

- Email `dmarc@kaven.com` não existe
- Filtros de spam bloqueando relatórios

**Solução:**

- Criar alias ou caixa de email real
- Usar serviço de análise DMARC (Postmark DMARC Analytics)

## Recursos Adicionais

### Ferramentas de Validação

- **MXToolbox:** [mxtoolbox.com](https://mxtoolbox.com/)
- **DMARC Analyzer:** [dmarcanalyzer.com](https://www.dmarcanalyzer.com/)
- **Mail Tester:** [mail-tester.com](https://www.mail-tester.com/)
- **Google Admin Toolbox:** [toolbox.googleapps.com](https://toolbox.googleapps.com/apps/checkmx/)

### Documentação Oficial

- **SPF:** [RFC 7208](https://www.rfc-editor.org/rfc/rfc7208)
- **DKIM:** [RFC 6376](https://www.rfc-editor.org/rfc/rfc6376)
- **DMARC:** [dmarc.org](https://dmarc.org/overview/)
- **BIMI:** [bimigroup.org](https://bimigroup.org/)

### Monitoramento de Reputação

- **Google Postmaster Tools:** [postmaster.google.com](https://postmaster.google.com/)
- **Microsoft SNDS:** [sendersupport.olc.protection.outlook.com](https://sendersupport.olc.protection.outlook.com/snds/)
- **Spamhaus:** [spamhaus.org](https://www.spamhaus.org/)

## Próximos Passos

Após configurar e validar o DNS:

1. ✅ Aguardar propagação completa (24-48h)
2. ✅ Executar scripts de validação
3. ✅ Enviar email de teste e verificar headers
4. ✅ Validar score no Mail Tester (target: 10/10)
5. ✅ Configurar webhooks no provedor
6. ✅ Iniciar warm-up period (30 dias)

## Relacionados

- [Email Integrations](/platform/email-integrations) - Configuração de provedores
- [Email Service](/platform/features/email-system) - Serviço de envio
- [Observability](/platform/observability) - Monitoramento de emails
