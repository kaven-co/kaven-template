{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm build --filter @kaven/api"
  },
  "deploy": {
    "startCommand": "cd apps/api && node dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
